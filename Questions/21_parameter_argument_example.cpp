#include <iostream>
using namespace std;

// Function definition
int add(int x, int y) {   // x, y → parameters
    return x + y;
}

int main() {

    // Function call
    cout << add(10, 30) << endl;  // 10, 30 → arguments

    return 0;
}
